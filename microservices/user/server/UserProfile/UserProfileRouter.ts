/*import { Router } from 'express';
import userProfileController from './Application/UserProfileController';

const router = Router();

router.get('/', userProfileController.getAllProfiles);  // GET /profiles
router.post('/', userProfileController.createProfile);  // POST /profiles

export default router;
*/
